// (1) http://tools.android.com/tech-docs/new-build-system/user-guide
// (2) http://tools.android.com/tech-docs/new-build-system/gradle-experimental

// Configures the code driving the build.  This only affects the code running the build,
// not the project. The project itself needs
// to declare its own repositories and dependencies. (1)

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle-experimental:0.2.0' // (2)
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
